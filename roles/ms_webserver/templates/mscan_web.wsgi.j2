'''
mod_wsgi script for running the MultiScanner web app
with Apache
'''

import sys

activate_this = '{{ multiscanner_venv }}/bin/activate_this.py'
with open(activate_this) as file_:
   exec(file_.read(), dict(__file__=activate_this))

sys.path.insert(0, '{{ multiscanner_install_prefix }}/multiscanner/multiscanner/web')

from app import app as application

